import type { ComponentSingleStyleConfig } from "@chakra-ui/theme";

const Link: ComponentSingleStyleConfig = {
    baseStyle: {
        color: "primary.base",
        width: "max-content",
    },
    defaultProps: {
        size: "sm",
    },
};

export default Link;
