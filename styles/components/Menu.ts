import type { ComponentSingleStyleConfig } from "@chakra-ui/theme";

const Menu: ComponentSingleStyleConfig = {
    baseStyle: {
        item: {
            color: "primary.base",
            fontWeight: 400,
        },
    },
};

export default Menu;
