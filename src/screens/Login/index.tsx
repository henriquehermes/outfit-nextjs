import { FC } from "react";
import { ContainerCustom } from "../../components/Container";

const LoginPage: FC = () => {
    return (
        <ContainerCustom>
            <div></div>
        </ContainerCustom>
    );
};

export default LoginPage;
